machine:
  node:
    version: 4.1.0
dependencies:
  post:
    - jspm install
deployment:
  production:
    branch: master
    commands:
      - npm run dist
      - surge --project dist/ --domain yslt.davidrapson.co.uk
