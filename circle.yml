dependencies:
  post:
    - jspm install
deployment:
  production:
    branch: master
    commands:
      - npm run update && npm run dist
      - surge --project dist/ --domain yslt.davidrapson.co.uk
